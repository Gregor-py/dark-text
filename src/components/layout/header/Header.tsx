import { Box, Button, Flex } from '@chakra-ui/react';
import React, { FC } from 'react';

const Header: FC = () => {
  return (
    <Flex>
      <Box>
        <Button></Button>
      </Box>
    </Flex>
  );
};

export default Header;